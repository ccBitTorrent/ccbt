# ccBitTorrent Environment Variables Configuration
# Canonical entries are sourced from ConfigManager._get_env_config.
# Legacy/unmapped entries are retained and marked explicitly as compatibility-only.
#
# Process environment vs .env file: ConfigManager only reads os.environ. A file named
# .env is NOT loaded unless you set CCBT_LOAD_DOTENV=true (merged before config; does
# not override variables already set in the process).

CCBT_LOAD_DOTENV=false  # If true, load .env before config (true/false) | legacy/unmapped (not in ConfigManager._get_env_config)
CCBT_DOTENV_PATH=  # Optional path to .env; default ./.env | legacy/unmapped (not in ConfigManager._get_env_config)

# =============================================================================
# NETWORK CONFIGURATION
# =============================================================================

CCBT_BLOCK_SIZE_KIB=16  # Block size in KiB (1-64)
CCBT_CIRCUIT_BREAKER_ENABLED=true  # Enable circuit breaker for peer connections
CCBT_CIRCUIT_BREAKER_FAILURE_THRESHOLD=5  # Number of failures before opening circuit breaker
CCBT_CIRCUIT_BREAKER_RECOVERY_TIMEOUT=60.0  # Recovery timeout for circuit breaker in seconds
CCBT_CONNECTION_HEALTH_CHECK_INTERVAL=30.0  # Interval between connection health checks in seconds
CCBT_CONNECTION_POOL_ADAPTIVE_LIMIT_ENABLED=true  # Enable adaptive connection limit calculation based on system resources and peer performance
CCBT_CONNECTION_POOL_ADAPTIVE_LIMIT_MAX=1000  # Maximum adaptive connection limit
CCBT_CONNECTION_POOL_ADAPTIVE_LIMIT_MIN=50  # Minimum adaptive connection limit
CCBT_CONNECTION_POOL_CPU_THRESHOLD=0.8  # CPU usage threshold (0.0-1.0) above which connection limit is reduced
CCBT_CONNECTION_POOL_GRACE_PERIOD=60.0  # Grace period in seconds for new connections before quality checks (allows time for bandwidth establishment)
# When true (default on Windows), ConfigManager clamps max_global_peers to 200,
# connection_pool_max_connections to 150, and max_peers_per_torrent to 100 regardless
# of higher CCBT_MAX_PEERS / CCBT_CONNECTION_POOL_MAX_CONNECTIONS / CCBT_MAX_PEERS_PER_TORRENT.
# Set false only if you accept possible socket instability on some Windows stacks.
CCBT_WINDOWS_NETWORK_COMPAT_STRICT=true  # legacy: unmapped to TOML; see comment block above
CCBT_CONNECTION_POOL_HEALTH_CHECK_INTERVAL=60.0  # Interval for connection health checks (seconds)
CCBT_CONNECTION_POOL_HEALTH_DEGRADATION_THRESHOLD=0.5  # Health score threshold (0.0-1.0) below which connection health level is degraded
CCBT_CONNECTION_POOL_HEALTH_RECOVERY_THRESHOLD=0.7  # Health score threshold (0.0-1.0) above which degraded connection health can recover
CCBT_CONNECTION_POOL_MAX_CONNECTIONS=200  # Maximum connections in connection pool
CCBT_CONNECTION_POOL_MAX_IDLE_TIME=300.0  # Maximum idle time before connection is closed (seconds)
CCBT_CONNECTION_POOL_MEMORY_THRESHOLD=0.8  # Memory usage threshold (0.0-1.0) above which connection limit is reduced
CCBT_CONNECTION_POOL_MIN_DOWNLOAD_BANDWIDTH=0.0  # Minimum download bandwidth in bytes/second for connections to be considered healthy (0 = disabled)
CCBT_CONNECTION_POOL_MIN_UPLOAD_BANDWIDTH=0.0  # Minimum upload bandwidth in bytes/second for connections to be considered healthy (0 = disabled)
CCBT_CONNECTION_POOL_PERFORMANCE_RECYCLING_ENABLED=true  # Enable performance-based connection recycling (recycle low-performing connections)
CCBT_CONNECTION_POOL_PERFORMANCE_THRESHOLD=0.3  # Performance score threshold (0.0-1.0) below which connections are recycled
CCBT_CONNECTION_POOL_QUALITY_THRESHOLD=0.3  # Minimum connection quality score (0.0-1.0) for connection reuse. Connections below this are recycled.
CCBT_CONNECTION_POOL_WARMUP_COUNT=10  # Number of connections to warmup on torrent start
CCBT_CONNECTION_POOL_WARMUP_ENABLED=true  # Enable connection warmup to pre-establish connections
CCBT_CONNECTION_TIMEOUT=30.0  # Connection timeout (1.0-300.0)
CCBT_CONNECTION_VALIDATION_ENABLED=true  # Enable connection state validation (BitTorrent spec compliant)
CCBT_DNS_CACHE_TTL=300  # DNS cache TTL in seconds (60-3600)
CCBT_ENABLE_FAIL_FAST_DHT=true  # Enable DHT fail-fast recovery when active peers are below min_peers_before_dht for longer than CCBT_FAIL_FAST_DHT_TIMEOUT
CCBT_FAIL_FAST_DHT_TIMEOUT=30.0  # Fail-fast timeout in seconds before triggering recovery DHT queries during low-peer state
# Peer discovery / recovery orchestration (optional; wired when present on network config model)
# CCBT_RECOVERY_REQUESTABLE_DEFICIT_WINDOW_S=12.0   # Min seconds swarm stays active-but-non-requestable before gated DHT escalation
# CCBT_RECOVERY_DHT_ESCALATION_COOLDOWN_S=10.0      # Min seconds between gated recovery DHT escalations (monotonic clock; first escalation not blocked at t=0)
# CCBT_RECOVERY_DHT_BATCH_DEFER_CYCLES=4            # Max peer_count_low cycles to defer while _dht_connect_deferral_active before forcing progression
# DHT candidate intel caps (per-torrent session fields; often set via API/options rather than env — see docs/en/network-troubleshooting.md)
# CCBT_DHT_CANDIDATE_PROMOTION_CAP=12
# CCBT_DHT_CANDIDATE_PROMOTION_CAP_DHT_ONLY=4
CCBT_GLOBAL_DOWN_KIB=0  # Global download limit (0+)
CCBT_GLOBAL_UP_KIB=0  # Global upload limit (0+)
CCBT_HANDSHAKE_TIMEOUT=10.0  # Handshake timeout (1.0-60.0)
# Adaptive handshake timeouts scale with swarm health (see network-troubleshooting.md).
CCBT_HANDSHAKE_ADAPTIVE_TIMEOUT_ENABLED=true  # Shorter timeouts when many peers; longer when few
CCBT_HANDSHAKE_TIMEOUT_DESPERATION_MIN=30.0  # Min handshake timeout when peer count is in desperation band
CCBT_HANDSHAKE_TIMEOUT_DESPERATION_MAX=60.0  # Max handshake timeout in desperation band
CCBT_HANDSHAKE_TIMEOUT_NORMAL_MIN=15.0  # Min in normal band
CCBT_HANDSHAKE_TIMEOUT_NORMAL_MAX=30.0  # Max in normal band
CCBT_HANDSHAKE_TIMEOUT_HEALTHY_MIN=20.0  # Min in healthy band
CCBT_HANDSHAKE_TIMEOUT_HEALTHY_MAX=40.0  # Max in healthy band
# Peer counts for health bands: effective=max(transport-live, post-handshake active) unless active_only.
CCBT_ADAPTIVE_TIMEOUT_HEALTH_PEER_SOURCE=effective  # effective | active_only
CCBT_ADAPTIVE_TIMEOUT_DESPERATION_MAX_PEERS=5  # Below this uses desperation timeouts
CCBT_ADAPTIVE_TIMEOUT_NORMAL_MAX_PEERS=20  # Below this uses normal band; at or above uses healthy band
CCBT_KEEP_ALIVE_INTERVAL=120.0  # Keep alive interval (30.0-600.0)
# Ports: CCBT_LISTEN_PORT_TCP = BitTorrent peer wire; CCBT_LISTEN_PORT_UDP = uTP/UDP peer listen if enabled;
# CCBT_DHT_PORT = DHT (UDP, BEP 5); CCBT_TRACKER_UDP_PORT = UDP tracker client (BEP 15). See docs/en/network-troubleshooting.md.
CCBT_LISTEN_PORT=64122  # Listen port (1024-65535) - deprecated: use listen_port_tcp and listen_port_udp
CCBT_LISTEN_PORT_TCP=64122  # TCP listen port for incoming peer connections (1024-65535)
CCBT_LISTEN_PORT_UDP=64122  # UDP listen port for incoming peer connections (1024-65535)
CCBT_MAX_CONCURRENT_CONNECTION_ATTEMPTS=20  # Maximum concurrent connection attempts to prevent OS socket exhaustion (BitTorrent spec compliant)
# Scale MSE initiator timeout when this torrent has zero active peers (1.0 = no change; e.g. 0.65 speeds preferred-mode plain fallback).
CCBT_MSE_INITIATOR_TIMEOUT_SCALE_ZERO_ACTIVE=1.0
CCBT_MAX_PEERS=200  # Maximum global peers (1-10000)
CCBT_MAX_PEERS_PER_TORRENT=50  # Maximum peers per torrent (1-1000)
CCBT_MAX_UPLOAD_SLOTS=4  # Maximum upload slots (1-20); raising can help reciprocal UNCHOKE from strict peers
# Tit-for-tat / reciprocation: prioritize upload to peers who choke us but have data we want; widen our unchoke when few remotes feed us
CCBT_RECIPROCATION_CHOKED_PEER_SCORE_BOOST=0.12  # Extra upload-slot score (0-0.5) when peer_choking and we are interested
CCBT_RECIPROCATION_REMOTE_NOT_INTERESTED_BOOST=0.06  # Extra score when remote not interested yet but we need their pieces
CCBT_LOW_DOWNLOAD_DIVERSITY_THRESHOLD=1  # If unchoked-by-remote count <= this, unchoke all active peers on our side (when full_unchoke true)
CCBT_LOW_DOWNLOAD_DIVERSITY_FULL_UNCHOKE=true  # Enable anti-deadlock full unchoke when download sources are scarce
CCBT_LOW_DOWNLOAD_DIVERSITY_USE_HYSTERESIS=false  # Stay in full-unchoke until sources exceed threshold + exit margin
CCBT_LOW_DOWNLOAD_DIVERSITY_EXIT_MARGIN=1  # Unchoked sources above threshold + margin required to exit hysteresis (1-10)
CCBT_LOW_DOWNLOAD_DIVERSITY_MAX_PEERS=0  # Cap peers unchoked in low-diversity mode (0 = no cap)
CCBT_LEECH_HEAVY_SWARM_TOTAL_UPLOAD_BPS_THRESHOLD=2048.0  # Sum of peers' upload_rate below this (B/s) uses leech-heavy choking weights
CCBT_INBOUND_UNKNOWN_HASH_WARNING_SAMPLE_INTERVAL=32  # WARNING every N unknown inbound hash events per prefix (2-10000)
CCBT_RECIPROCATION_MAX_COMBINED_BOOST=0.25  # Cap sum of choked + remote-not-interested reciprocation bonuses per peer (0-1)
CCBT_OPTIMISTIC_UNCHOKE_TOP_CANDIDATES=3  # Pool size for optimistic unchoke selection (1-16)
CCBT_OPTIMISTIC_UNCHOKE_USE_JITTER=true  # Random among top candidates; false = deterministic tie-break
# Remote UNCHOKE stall recovery: base timeout, seed-anchor timeout, solo/all-choked grace (see docs/en/network-troubleshooting.md)
CCBT_PEER_CHOKED_HARD_TIMEOUT_SECONDS=30.0  # Base seconds before hard recovery if still remote-choked (5-600)
CCBT_PEER_CHOKED_ANCHOR_TIMEOUT_SECONDS=75.0  # Seed-anchor peers get this longer window unless underperforming (10-900)
CCBT_PEER_CHOKED_SOLO_GRACE_SECONDS=180.0  # Min grace when you are alone or nobody is requestable yet (30-3600); lowering rotates peers faster but risks zero connections
CCBT_PEER_CHOKED_SOLO_GRACE_ZERO_BYTES_CAP_SECONDS=0  # If >0 and zero bytes/outstanding, cap solo grace at this (0=disabled)
CCBT_METADATA_EXCHANGE_TIMEOUT=60.0  # Metadata exchange timeout in seconds (BEP 9 compliant)
CCBT_METADATA_PIECE_TIMEOUT=15.0  # Timeout per metadata piece request in seconds
CCBT_PEER_VALIDATION_ENABLED=true  # Enable peer validation before accepting connections
CCBT_PER_PEER_DOWN_KIB=0  # Per-peer download limit (0+)
CCBT_PER_PEER_UP_KIB=0  # Per-peer upload limit (0+)
CCBT_PIPELINE_ADAPTIVE_DEPTH=true  # Enable adaptive pipeline depth based on connection latency
CCBT_PIPELINE_COALESCE_THRESHOLD_KIB=4  # Maximum gap in KiB for coalescing adjacent requests
CCBT_PIPELINE_DEPTH=16  # Request pipeline depth (1-128)
CCBT_PIPELINE_ENABLE_COALESCING=true  # Enable request coalescing (combine adjacent requests)
CCBT_PIPELINE_ENABLE_PRIORITIZATION=true  # Enable request prioritization (rarest pieces first)
CCBT_PIPELINE_MAX_DEPTH=128  # Maximum pipeline depth (increased for better throughput)
CCBT_PIPELINE_MIN_DEPTH=4  # Minimum pipeline depth
CCBT_PEER_QUALITY_PROBATION_TIMEOUT=60.0  # Seconds before dropping probation peers without bitfield/HAVE/data (8-600)
CCBT_PEER_QUALITY_PROBATION_SPARSE_CHOKE_GRACE_SECONDS=90.0  # Additional sparse-swarm grace before dropping active-but-choking probation peers
CCBT_PEER_RECYCLE_SPARSE_BACKOFF_CAP_SECONDS=10.0  # Cap stale-unchoke backoff in sparse swarms to keep recycle pressure responsive
CCBT_RECYCLE_PRESSURE_THRESHOLD=0.8  # Sparse recycle pressure threshold (active/capacity ratio, 0.0-1.0)
CCBT_PROTOCOL_V2_ENABLE=true  # Enable BitTorrent Protocol v2 support (BEP 52) (true/false)
CCBT_PROTOCOL_V2_HANDSHAKE_TIMEOUT=30.0  # v2 handshake timeout in seconds (5.0-300.0)
CCBT_PROTOCOL_V2_PREFER=false  # Prefer v2 protocol when both v1 and v2 are available (true/false)
CCBT_PROTOCOL_V2_SUPPORT_HYBRID=true  # Support hybrid torrents (both v1 and v2 metadata) (true/false)
CCBT_RETRY_BASE_DELAY=10.0  # Base delay for retry backoff in seconds (standard: 10s)
CCBT_RETRY_EXPONENTIAL_BACKOFF=true  # Use exponential backoff for retries
CCBT_RETRY_MAX_DELAY=300.0  # Maximum delay for retry backoff in seconds
CCBT_SEND_BITFIELD_AFTER_METADATA=true  # Send bitfield to peers after metadata exchange completes (BEP 3 compliant)
CCBT_SEND_INTERESTED_AFTER_METADATA=true  # Send INTERESTED message after metadata exchange completes (BEP 3 compliant)
CCBT_SOCKET_ADAPTIVE_BUFFERS=true  # Enable adaptive socket buffer sizing based on BDP
CCBT_SOCKET_ENABLE_WINDOW_SCALING=true  # Enable TCP window scaling for high-speed connections
CCBT_SOCKET_MAX_BUFFER_KIB=65536  # Maximum socket buffer size in KiB
CCBT_SOCKET_MIN_BUFFER_KIB=64  # Minimum socket buffer size in KiB
CCBT_TIMEOUT_ADAPTIVE=true  # Enable adaptive timeout calculation based on RTT
CCBT_TIMEOUT_MAX_SECONDS=300.0  # Maximum timeout in seconds
CCBT_TIMEOUT_MIN_SECONDS=5.0  # Minimum timeout in seconds
CCBT_TIMEOUT_RTT_MULTIPLIER=3.0  # RTT multiplier for timeout calculation
CCBT_TRACKER_DNS_CACHE_TTL=300  # Tracker DNS cache TTL in seconds
CCBT_TRACKER_ENABLE_DNS_CACHE=true  # Enable DNS caching for tracker requests
CCBT_TRACKER_KEEPALIVE_TIMEOUT=300.0  # Tracker HTTP keepalive timeout in seconds
CCBT_TRACKER_NETWORK_FAILURE_QUARANTINE_SECONDS=90.0  # Quarantine after repeated network-class tracker failures (s)
CCBT_TRACKER_PAYLOAD_FAILURE_QUARANTINE_SECONDS=120.0  # Quarantine after invalid tracker payloads (s)
CCBT_TRACKER_DNS_REFUSED_ESCALATION_STREAK=5  # Consecutive failures before DNS/refused quarantine escalates (2-100)
CCBT_TRACKER_ZERO_ACTIVE_BATCHES_BEFORE_DHT_SHORT_CIRCUIT=3  # Zero-active batches before DHT short-circuit (1-20)
CCBT_TRACKER_TIMEOUT=30.0  # Tracker request timeout (5.0-120.0)
CCBT_TRACKER_UDP_PORT=64123  # UDP port for tracker client communication (1024-65535)
CCBT_UTP_ACK_INTERVAL=0.1  # ACK packet send interval in seconds (0.01-1.0)
CCBT_UTP_CONNECTION_TIMEOUT=30.0  # uTP connection timeout in seconds (5.0-300.0)
CCBT_UTP_INITIAL_RATE=1500  # Initial send rate in bytes/second (1024-100000)
CCBT_UTP_MAX_RATE=1000000  # Maximum send rate in bytes/second (10000-10000000)
CCBT_UTP_MAX_RETRANSMITS=10  # Maximum retransmission attempts before connection failure (3-50)
CCBT_UTP_MAX_WINDOW_SIZE=65535  # Maximum uTP receive window size in bytes (8192-65535)
CCBT_UTP_MIN_RATE=512  # Minimum send rate in bytes/second (256-10000)
CCBT_UTP_MTU=1200  # uTP MTU size (maximum UDP packet size) (576-65507)
CCBT_UTP_PREFER_OVER_TCP=true  # Prefer uTP over TCP when both are supported (true/false)
CCBT_UTP_RETRANSMIT_TIMEOUT_FACTOR=4.0  # RTT multiplier for retransmit timeout (2.0-10.0)
CCBT_XET_MULTICAST_ADDRESS=239.255.255.250  # XET multicast address for local network discovery
CCBT_XET_MULTICAST_PORT=6882  # XET multicast port
CCBT_XET_PORT=  # XET protocol port (uses listen_port_udp if not set)

# =============================================================================
# STRATEGY CONFIGURATION
# =============================================================================

CCBT_ADAPTIVE_HYBRID_PHASE_DETECTION_WINDOW=10  # Number of pieces to analyze for phase detection in adaptive hybrid mode
CCBT_BANDWIDTH_WEIGHTED_RAREST_WEIGHT=0.7  # Weight for bandwidth in bandwidth-weighted rarest-first (0.0=rarity only, 1.0=bandwidth only)
CCBT_ENDGAME_DUPLICATES=2  # Endgame duplicate requests (1-10)
CCBT_ENDGAME_THRESHOLD=0.95  # Endgame mode threshold (0.1-1.0)
CCBT_PIECE_SELECTION=rarest_first  # Piece selection strategy
CCBT_PROGRESSIVE_RAREST_TRANSITION_THRESHOLD=0.5  # Progress threshold for transitioning from sequential to rarest-first in progressive mode
CCBT_STREAMING_MODE=false  # Enable streaming mode (true/false)

# =============================================================================
# DISK CONFIGURATION
# =============================================================================

CCBT_ATTRIBUTES_APPLY_EXECUTABLE_BIT=true  # Set executable bit for files with attr='x' (true/false)
CCBT_ATTRIBUTES_APPLY_HIDDEN_ATTR=true  # Apply hidden attribute for files with attr='h' (Windows) (true/false)
CCBT_ATTRIBUTES_APPLY_SYMLINKS=true  # Create symlinks for files with attr='l' (true/false)
CCBT_ATTRIBUTES_PRESERVE_ATTRIBUTES=true  # Preserve file attributes (executable, hidden, symlinks) (true/false)
CCBT_ATTRIBUTES_SKIP_PADDING_FILES=true  # Skip downloading padding files (BEP 47) (true/false)
CCBT_ATTRIBUTES_VERIFY_FILE_SHA1=false  # Verify file SHA-1 hashes when provided (BEP 47) (true/false)
CCBT_AUTO_RESUME=true  # Automatically resume from checkpoint on startup (true/false)
CCBT_CHECKPOINT_COMPRESSION=true  # Compress binary checkpoint files (true/false)
CCBT_CHECKPOINT_DIR=  # Checkpoint directory (defaults to download_dir/.ccbt/checkpoints)
CCBT_CHECKPOINT_ENABLED=true  # Enable download checkpointing (true/false)
CCBT_DIRECT_IO=false  # Use direct I/O (true/false)
CCBT_DISK_WORKERS=2  # Number of disk I/O workers (1-16)
CCBT_FAST_RESUME_ENABLED=true  # Enable fast resume support (true/false)
CCBT_HASH_WORKERS=4  # Number of hash verification workers (1-32)
CCBT_MMAP_CACHE_MB=128  # Memory-mapped cache size in MB (16-2048)
CCBT_PREALLOCATE=full  # Preallocation strategy
CCBT_READ_AHEAD_KIB=64  # Read ahead size in KiB (0-1024)
CCBT_RESUME_DATA_FORMAT_VERSION=1  # Resume data format version (1-100)
CCBT_RESUME_SAVE_INTERVAL=30.0  # Interval to save resume data in seconds (1.0-3600.0)
CCBT_RESUME_VERIFY_ON_LOAD=true  # Verify resume data integrity on load (true/false)
CCBT_RESUME_VERIFY_PIECES=10  # Number of pieces to verify on resume (0-100, 0 = disable)
CCBT_SYNC_WRITES=false  # Synchronize writes (true/false)
CCBT_USE_MMAP=true  # Use memory mapping (true/false)
CCBT_WRITE_BATCH_KIB=64  # Write batch size in KiB (1-1024)
CCBT_XET_CACHE_DB_PATH=  # Path to Xet deduplication cache database (defaults to download_dir/.xet_cache/chunks.db)
CCBT_XET_CHUNK_MAX_SIZE=131072  # Maximum Xet chunk size in bytes (32768-524288)
CCBT_XET_CHUNK_MIN_SIZE=8192  # Minimum Xet chunk size in bytes (4096-65536)
CCBT_XET_CHUNK_STORE_PATH=  # Path to Xet chunk storage directory (defaults to download_dir/.xet_chunks)
CCBT_XET_CHUNK_TARGET_SIZE=16384  # Target Xet chunk size in bytes (8192-65536)
CCBT_XET_COMPRESSION_ENABLED=false  # Enable LZ4 compression for stored chunks (true/false)
CCBT_XET_DEDUPLICATION_ENABLED=true  # Enable chunk-level deduplication (true/false)
CCBT_XET_ENABLED=false  # Enable Xet protocol for content-defined chunking and deduplication (true/false)
CCBT_XET_USE_P2P_CAS=true  # Use peer-to-peer Content Addressable Storage (DHT-based) (true/false)

# =============================================================================
# DISCOVERY CONFIGURATION
# =============================================================================

CCBT_AGGRESSIVE_DISCOVERY_ACTIVE_THRESHOLD_KIB=1.0  # Minimum download rate (KB/s) to enable aggressive discovery mode
CCBT_AGGRESSIVE_DISCOVERY_INTERVAL_ACTIVE=30.0  # DHT query interval in seconds for actively downloading torrents (>1KB/s, minimum 30s)
CCBT_AGGRESSIVE_DISCOVERY_INTERVAL_POPULAR=60.0  # DHT query interval in seconds for popular torrents (20+ peers, minimum 30s)
CCBT_AGGRESSIVE_DISCOVERY_MAX_PEERS_PER_QUERY=100  # Maximum peers to query per DHT query in aggressive mode
CCBT_AGGRESSIVE_DISCOVERY_POPULAR_THRESHOLD=20  # Minimum peer count to enable aggressive discovery mode
CCBT_AGGRESSIVE_INITIAL_DHT_INTERVAL=30.0  # Initial DHT query interval in seconds when aggressive mode is enabled (for first 5 minutes, minimum 30s)
CCBT_AGGRESSIVE_INITIAL_DISCOVERY=true  # Enable aggressive initial discovery mode (shorter intervals for first few announces/queries)
CCBT_AGGRESSIVE_INITIAL_TRACKER_INTERVAL=30.0  # Initial tracker announce interval in seconds when aggressive mode is enabled (for first 5 minutes)
CCBT_DEFAULT_TRACKERS=https://tracker.opentrackr.org:443/announce,https://tracker.torrent.eu.org:443/announce,https://tracker.openbittorrent.com:443/announce,http://tracker.opentrackr.org:1337/announce,http://tracker.openbittorrent.com:80/announce,udp://tracker.opentrackr.org:1337/announce,udp://tracker.openbittorrent.com:80/announce  # Default trackers to use for magnet links without tr= parameters
CCBT_DHT_BOOTSTRAP_NODES=router.bittorrent.com:6881,dht.transmissionbt.com:6881,router.utorrent.com:6881,dht.libtorrent.org:25401  # Mapped to discovery.dht_bootstrap_nodes
CCBT_DHT_ADAPTIVE_INTERVAL_ENABLED=true  # Enable adaptive DHT lookup intervals based on swarm health
CCBT_DHT_ADAPTIVE_INTERVAL_MAX=1920.0  # Maximum adaptive DHT refresh interval in seconds (32 minutes for standard exponential backoff)
CCBT_DHT_ADAPTIVE_INTERVAL_MIN=60.0  # Minimum adaptive DHT refresh interval in seconds
CCBT_DHT_AGGRESSIVE_ALPHA=8  # Number of parallel queries for aggressive DHT lookups (BEP 5 alpha parameter)
CCBT_DHT_AGGRESSIVE_K=32  # Bucket size for aggressive DHT lookups (BEP 5 k parameter)
CCBT_DHT_AGGRESSIVE_MAX_DEPTH=15  # Maximum depth for aggressive DHT iterative lookups
CCBT_DHT_BASE_REFRESH_INTERVAL=600.0  # Base DHT refresh interval in seconds (used when adaptive is disabled or as base for adaptive calculation)
CCBT_DHT_ENABLE_INDEXING=true  # Enable infohash indexing (BEP 51) (true/false)
CCBT_DHT_ENABLE_IPV6=true  # Enable IPv6 DHT support (BEP 32) (true/false)
CCBT_DHT_ENABLE_MULTIADDRESS=true  # Enable multi-address support (BEP 45) (true/false)
CCBT_DHT_ENABLE_STORAGE=false  # Enable DHT storage (BEP 44) (true/false)
CCBT_DHT_INDEX_SAMPLES_PER_KEY=8  # Maximum samples per index key (BEP 51) (1-100)
CCBT_DHT_IPV6_BOOTSTRAP_NODES=  # IPv6 DHT bootstrap nodes (format: [hostname:port or [IPv6]:port])
CCBT_DHT_MAX_ADDRESSES_PER_NODE=4  # Maximum addresses to track per node (BEP 45) (1-16)
CCBT_DHT_MAX_STORAGE_SIZE=1000  # Maximum storage value size in bytes (BEP 44) (100-10000)
CCBT_DHT_NORMAL_ALPHA=5  # Number of parallel queries for normal DHT lookups (BEP 5 alpha parameter)
CCBT_DHT_NORMAL_K=16  # Bucket size for normal DHT lookups (BEP 5 k parameter)
CCBT_DHT_NORMAL_MAX_DEPTH=12  # Maximum depth for normal DHT iterative lookups
CCBT_DHT_PORT=64120  # DHT port (1024-65535)
CCBT_DHT_PREFER_IPV6=false  # Prefer IPv6 addresses over IPv4 when available (true/false)
CCBT_DHT_QUALITY_RESPONSE_TIME_WINDOW=10  # Number of recent response times to track per node for quality calculation
CCBT_DHT_QUALITY_TRACKING_ENABLED=true  # Enable DHT node quality tracking (response times, success rates)
# DHT query timeouts can adapt to swarm health (same peer-count bands as handshake when enabled).
CCBT_DHT_ADAPTIVE_TIMEOUT_ENABLED=true
CCBT_DHT_TIMEOUT_DESPERATION_MIN=30.0
CCBT_DHT_TIMEOUT_DESPERATION_MAX=60.0
CCBT_DHT_TIMEOUT_NORMAL_MIN=5.0
CCBT_DHT_TIMEOUT_NORMAL_MAX=15.0
CCBT_DHT_TIMEOUT_HEALTHY_MIN=10.0
CCBT_DHT_TIMEOUT_HEALTHY_MAX=30.0
CCBT_DHT_READONLY_MODE=false  # Enable read-only DHT mode (BEP 43) (true/false)
CCBT_DHT_STORAGE_TTL=3600  # Storage TTL in seconds (BEP 44) (60-86400)
CCBT_DISCOVERY_CACHE_TTL=60.0  # Discovery result cache TTL in seconds
CCBT_ENABLE_DHT=true  # Enable DHT (true/false)
CCBT_ENABLE_HTTP_TRACKERS=true  # Enable HTTP trackers (true/false)
CCBT_ENABLE_PEX=true  # Enable Peer Exchange (true/false)
CCBT_ENABLE_UDP_TRACKERS=true  # Enable UDP trackers (true/false)
CCBT_MIN_PEERS_BEFORE_DHT=10  # Minimum active peers before starting DHT discovery (0 = allow DHT immediately as fallback)
CCBT_BOOTSTRAP_SEED_REPLAY_LIMIT=6  # Bootstrap seed nodes to try per retry cycle (1-20)
CCBT_DHT_BOOTSTRAP_RETRIES_MAX=3  # Max rebootstrap attempts before backoff (1-20)
CCBT_BOOTSTRAP_RETRY_MEMO_TTL_S=30.0  # Replay memo TTL between bootstrap retries for same reason (s)
CCBT_DHT_BOOTSTRAP_MEMO_TTL_S=120.0  # Memo TTL for zero-state bootstrap recovery suppression (s)
CCBT_DHT_DNS_HOST_BACKOFF_INITIAL_S=2.0  # Initial DNS failure backoff per bootstrap host (s)
CCBT_DHT_DNS_HOST_BACKOFF_MULTIPLIER=2.0  # Per-host DNS backoff multiplier (1.2-8)
CCBT_DHT_DNS_HOST_BACKOFF_MAX_S=120.0  # Max per-host DNS failure backoff (s)
CCBT_DHT_ZERO_STATE_REPROBE_WAIT_S=45.0  # Base wait before retry when DHT routing table is empty (s)
CCBT_DHT_EMPTY_STATE_BACKOFF_FACTOR=1.5  # Multiplier for repeated empty DHT bootstrap outcomes (1-10)
CCBT_DHT_REBOOTSTRAP_TIMEOUT_S=45.0  # Timeout for periodic DHT rebootstrap fallback (s)
CCBT_DHT_BOOTSTRAP_TIMEOUT_S=45.0  # Timeout for forced bootstrap when ensuring node coverage (s)
CCBT_LOW_PEER_THRESHOLD=1  # Active-peer threshold for low-peer recovery (0-20)
CCBT_LOW_PEER_SUPPRESSION_WINDOW_S=20.0  # Suppress repeated low-peer recovery within this window (s)
# Require a usable download/metadata path before skipping DHT after tracker handoff (not TCP alone)
CCBT_PEER_COUNT_LOW_SKIP_DHT_REQUIRES_USABLE_PATH=true
# Requestable-peer-driven discovery (target / tick / DHT pressure when peers are active-but-choked)
CCBT_REQUESTABLE_DRIVEN_DISCOVERY_ENABLED=true
CCBT_TARGET_REQUESTABLE_PEERS=12
CCBT_REQUESTABLE_TICK_INTERVAL_S=15.0
CCBT_REQUESTABLE_FORCE_DHT_WHEN_ZERO=true
# DHT / requestable-driven discovery connect pressure per tick (see dht_setup burst_cap).
CCBT_MAX_CONNECT_BURST_PER_TICK=16
# UDP/HTTP immediate tracker callback: max peers passed to connect_peers_to_download per
# response; remainder go to the peer manager pending queue (`ccbt/session/session.py`).
CCBT_TRACKER_IMMEDIATE_CONNECT_BURST_TOTAL=24
CCBT_TRACKER_IMMEDIATE_CONNECT_BURST_PER_SOURCE=24
# Circuit breaker for immediate callbacks (zero-active streak): max callbacks within window_s
# before deferring peers to the pending queue instead of dropping them.
CCBT_TRACKER_IMMEDIATE_CONNECT_WINDOW_S=20.0
CCBT_TRACKER_IMMEDIATE_CONNECT_WINDOW_CAP=6
# Per tracker_url|peer_source cap within immediate batching: half_max_peers (default, legacy)
# uses max(1, max_peers_per_torrent//2); full_max_peers uses min(burst_per_source, max_peers).
CCBT_TRACKER_IMMEDIATE_PER_SOURCE_CAP_MODE=half_max_peers
# After `config_conditional` runs, interface count may further adjust max_global_peers (e.g. 200
# on a single NIC). Layer order: env merge -> Windows strict caps -> conditional optimizations.
# Keep CCBT_TARGET_REQUESTABLE_PEERS within realistic global limits when strict mode is on
# (effective max_global_peers is 200 across all torrents on Windows).
# Inbound TCP registration (multi-torrent / magnet — see docs/en/network-troubleshooting.md)
CCBT_INBOUND_MAX_PROBATION_INFLIGHT_PER_HASH=8
CCBT_INBOUND_REGISTRATION_WAIT_CAP_NO_SESSIONS_S=60.0
CCBT_INBOUND_REGISTRATION_WAIT_CAP_DEFAULT_S=15.0
CCBT_INBOUND_REGISTRATION_WAIT_CAP_STORM_S=8.0
CCBT_INBOUND_REGISTRATION_WAIT_CAP_METADATA_PENDING_S=60.0
CCBT_INBOUND_GRACE_POLL_SECONDS_NO_SESSIONS_S=8.0
CCBT_INBOUND_GRACE_POLL_SECONDS_STORM_S=1.5
CCBT_INBOUND_GRACE_POLL_SECONDS_DEFAULT_S=2.5
CCBT_INBOUND_PROBATION_WINDOW_S=8.0
CCBT_INBOUND_PROBATION_WINDOW_STORM_S=4.0
CCBT_INBOUND_PROBATION_RETRY_INTERVAL_S=0.5
CCBT_INBOUND_UNKNOWN_HASH_STORM_THRESHOLD=12
# Global cap on peers waiting for a per-hash probation slot (0 = queue off, grace-poll only)
CCBT_INBOUND_PROBATION_WAIT_QUEUE_MAX_TOTAL=256
CCBT_INBOUND_PROBATION_QUEUED_MAX_WAIT_S=120.0  # Max queued probation wait time before expiry (0=disabled)
# Experimental: blend ccbt.ml.peer_selector scores into outbound peer ranking (0 = off, max 0.5)
CCBT_PEER_SELECTOR_ML_RANKING_WEIGHT=0.0
# Optional: disconnect oldest choked peers to free slots when nobody is requestable (default off)
CCBT_CHOKE_ONLY_SLOT_REPLACEMENT_ENABLED=false
CCBT_CHOKE_ONLY_SLOT_REPLACEMENT_MIN_ACTIVE_PEERS=4
CCBT_CHOKE_ONLY_SLOT_REPLACEMENT_MIN_CHOKE_RATIO=0.85
CCBT_CHOKE_ONLY_SLOT_REPLACEMENT_MAX_DISCONNECT_FRACTION=0.15
CCBT_CHOKE_ONLY_SLOT_REPLACEMENT_AT_LIMIT_FRACTION=0.95
CCBT_PEX_INTERVAL=60.0  # Peer Exchange announce interval in seconds (30.0-3600.0, BEP 11: max 1 per minute)
CCBT_STRICT_PRIVATE_MODE=true  # Enforce strict BEP 27 rules for private torrents (true/false)
CCBT_TRACKER_ADAPTIVE_INTERVAL_ENABLED=true  # Enable adaptive tracker announce intervals based on performance and peer count
CCBT_TRACKER_ADAPTIVE_INTERVAL_MAX=3600.0  # Maximum adaptive tracker announce interval in seconds
CCBT_TRACKER_ADAPTIVE_INTERVAL_MIN=20.0  # Minimum adaptive tracker announce interval in seconds
CCBT_TRACKER_ANNOUNCE_INTERVAL=60.0  # Tracker announce interval in seconds (60.0-86400.0)
CCBT_TRACKER_AUTO_SCRAPE=true  # Automatically scrape trackers when adding torrents (true/false)
CCBT_TRACKER_BASE_ANNOUNCE_INTERVAL=1800.0  # Base tracker announce interval in seconds (used when adaptive is disabled or as base for adaptive calculation)
CCBT_TRACKER_PEER_COUNT_WEIGHT=0.3  # Weight for peer count in tracker performance ranking (0.0-1.0)
CCBT_TRACKER_PERFORMANCE_WEIGHT=0.4  # Weight for performance metrics in tracker performance ranking (0.0-1.0)
CCBT_TRACKER_SCRAPE_INTERVAL=45.0  # Tracker scrape interval in seconds (60.0-86400.0)
CCBT_TRACKER_STOPPED_ANNOUNCE_TIMEOUT_S=5.0  # Budget for event=stopped announces on session shutdown (s)
CCBT_XET_CHUNK_QUERY_BATCH_SIZE=50  # Batch size for parallel chunk queries
CCBT_XET_CHUNK_QUERY_MAX_CONCURRENT=50  # Maximum concurrent chunk queries

# =============================================================================
# MEDIA CONFIGURATION
# =============================================================================

CCBT_ENABLE_INLINE_MEDIA_PREVIEW=false  # Enable experimental inline terminal-native media preview features
CCBT_ENABLE_MEDIA_STREAMING=true  # Enable daemon-backed local media streaming support
CCBT_INLINE_MEDIA_PREVIEW_MODE=disabled  # Preview mode for future inline media experiments
CCBT_MEDIA_ASSUMED_BITRATE_BPS=1000000  # Fallback bitrate estimate used for streaming prioritization
CCBT_MEDIA_BIND_HOST=127.0.0.1  # Bind host for local media stream servers
CCBT_MEDIA_DEFAULT_PORT=0  # Preferred media stream port (0 selects an ephemeral port)
CCBT_MEDIA_REQUEST_WAIT_TIMEOUT_SECONDS=5.0  # Maximum wait for a requested byte range to become available
CCBT_MEDIA_STARTUP_BUFFER_SECONDS=8.0  # Minimum buffered playback lead before a stream is marked ready
CCBT_MEDIA_STREAM_CHUNK_SIZE_KIB=256  # Chunk size used when serving HTTP byte ranges
CCBT_MEDIA_TOKEN_TTL_SECONDS=3600.0  # Lifetime for generated media stream access tokens
CCBT_MEDIA_VLC_EXECUTABLE_PATH=  # Optional absolute path to the VLC executable

# =============================================================================
# SECURITY CONFIGURATION
# =============================================================================

CCBT_BLACKLIST_AUTO_UPDATE_ENABLED=false  # Blacklist configuration
CCBT_BLACKLIST_AUTO_UPDATE_INTERVAL=3600.0  # Blacklist configuration
CCBT_BLACKLIST_AUTO_UPDATE_SOURCES=  # Blacklist configuration
CCBT_BLACKLIST_DEFAULT_EXPIRATION_HOURS=  # Blacklist configuration
CCBT_BLACKLIST_ENABLE_PERSISTENCE=true  # Blacklist configuration
CCBT_BLACKLIST_FILE=~/.ccbt/security/blacklist.json  # Blacklist configuration
CCBT_BLACKLIST_LOCAL_SOURCE_ENABLED=true  # Blacklist configuration
CCBT_BLACKLIST_LOCAL_SOURCE_EVALUATION_INTERVAL=300.0  # Blacklist configuration
CCBT_BLACKLIST_LOCAL_SOURCE_EXPIRATION_HOURS=24.0  # Blacklist configuration
CCBT_BLACKLIST_LOCAL_SOURCE_METRIC_WINDOW=3600.0  # Blacklist configuration
CCBT_BLACKLIST_LOCAL_SOURCE_MIN_OBSERVATIONS=3  # Blacklist configuration
# MSE/PE (BEP 3): obfuscated handshake on TCP; plaintext fallback only if allowed.
# Canonical: security.enable_encryption (Config merges network.enable_encryption to match).
# CCBT_ENCRYPTION_MODE alone does not enable MSE; set CCBT_ENABLE_ENCRYPTION=true as well.
CCBT_ENABLE_ENCRYPTION=true  # Enable MSE/PE for peer TCP (true/false)
CCBT_NETWORK_ENABLE_ENCRYPTION=true  # Mirrors [network] enable_encryption (synced to security.enable_encryption at load)
CCBT_ENABLE_IP_FILTER=true  # Enable IP filtering (true/false)
CCBT_ENABLE_SSL_PEERS=true  # Enable SSL for peer connections (true/false)
CCBT_ENABLE_SSL_TRACKERS=true  # Enable SSL for tracker connections (true/false)
CCBT_ENCRYPTION_ALLOWED_CIPHERS=rc4,aes  # Allowed ciphers (comma-separated: rc4,aes,chacha20)
CCBT_ENCRYPTION_ALLOW_PLAIN_FALLBACK=true  # Allow fallback to plain connection (true/false)
CCBT_ENCRYPTION_DH_KEY_SIZE=768  # DH key size in bits: 768 or 1024
CCBT_ENCRYPTION_MODE=preferred  # Encryption mode: disabled/preferred/required
CCBT_ENCRYPTION_PREFER_RC4=true  # Prefer RC4 cipher for compatibility (true/false)
CCBT_FILTER_CACHE_DIR=~/.ccbt/filters  # Filter cache directory
CCBT_FILTER_FILES=  # Comma-separated filter file paths
CCBT_FILTER_LOG_BLOCKED=true  # Log blocked connections (true/false)
CCBT_FILTER_MODE=block  # Filter mode: block or allow
CCBT_FILTER_UPDATE_INTERVAL=86400.0  # Update interval in seconds (3600.0-604800.0)
CCBT_FILTER_URLS=  # Comma-separated filter list URLs
CCBT_MAX_CONNECTIONS_PER_PEER=1  # Maximum parallel connections per peer (1-8)
CCBT_PEER_QUALITY_THRESHOLD=0.3  # Minimum reputation score (0.0-1.0) for peers to be accepted during discovery. Peers below this threshold are filtered out.
CCBT_RATE_LIMIT_ENABLED=true  # Enable security rate limiter (true/false)
CCBT_SSL_ALLOW_INSECURE_PEERS=true  # Allow insecure peers for opportunistic encryption (true/false)
CCBT_SSL_CA_CERTIFICATES=  # Path to CA certificates file or directory
CCBT_SSL_CLIENT_CERTIFICATE=  # Path to client certificate file (PEM format)
CCBT_SSL_CLIENT_KEY=  # Path to client private key file (PEM format)
CCBT_SSL_PROTOCOL_VERSION=TLSv1.3  # TLS protocol version (TLSv1.2, TLSv1.3, PROTOCOL_TLS)
CCBT_SSL_VERIFY_CERTIFICATES=true  # Verify SSL certificates (true/false)
CCBT_VALIDATE_PEERS=true  # Validate peers before exchanging data (true/false)
CCBT_AUTHENTICATED_SWARMS_MODE=opportunistic  # Authenticated swarm mode: off, opportunistic, strict
CCBT_AUTHENTICATED_SWARMS_DISCOVERY_MODE=full  # Discovery mode for authenticated swarms: full, trackers_only, dht_only, pex_off
CCBT_AUTHENTICATED_SWARMS_DISCOVERY_STRICT_FOR_STRICT_MODE=true  # Enforce full discovery restrictions in strict mode
CCBT_AUTHENTICATED_SWARMS_STRICT_LTEP_TIMEOUT_S=30.0  # Timeout to wait for inbound LTEP extension handshake in strict mode
CCBT_AUTHENTICATED_SWARMS_TRUSTED_IDS=  # Comma-separated list of trusted swarm IDs (hex/uuid/base32)
CCBT_AUTHENTICATED_SWARMS_FAIL_CLOSED_ON_PARSE_ERRORS=false  # Fail closed on parse failures in strict mode (true/false)
CCBT_AUTHENTICATED_SWARMS_TRUST_STORE_PATH=  # Optional path to swarm trust store JSON
CCBT_AUTHENTICATED_SWARMS_TRUST_STORE_REFRESH_INTERVAL_S=60.0  # Trust store refresh interval in seconds
CCBT_AUTHENTICATED_SWARMS_REVOCATION_PROFILE_PATH=  # Optional path to revocation profile JSON
CCBT_AUTHENTICATED_SWARMS_REVOCATION_REFRESH_INTERVAL_S=300.0  # Revocation profile refresh interval in seconds

# =============================================================================
# OBSERVABILITY CONFIGURATION
# =============================================================================

CCBT_ENABLE_METRICS=true  # Enable metrics collection (true/false)
CCBT_ENABLE_PEER_TRACING=false  # Enable peer tracing (true/false)
CCBT_EVENT_BUS_BATCH_SIZE=50  # Maximum number of events to process per batch
CCBT_EVENT_BUS_BATCH_TIMEOUT=0.05  # Timeout in seconds to wait when collecting a batch
CCBT_EVENT_BUS_EMIT_TIMEOUT=0.01  # Timeout in seconds when trying to emit to a full queue
CCBT_EVENT_BUS_MAX_QUEUE_SIZE=10000  # Maximum size of event queue
CCBT_EVENT_BUS_QUEUE_FULL_THRESHOLD=0.9  # Queue fullness threshold (0.0-1.0) for dropping low-priority events
CCBT_EVENT_BUS_THROTTLE_DHT_NODE_ADDED=0.1  # Throttle interval for dht_node_added events (max events per second = 1/interval)
CCBT_EVENT_BUS_THROTTLE_DHT_NODE_FOUND=0.1  # Throttle interval for dht_node_found events (max events per second = 1/interval)
CCBT_EVENT_BUS_THROTTLE_GLOBAL_METRICS_UPDATE=0.5  # Throttle interval for global_metrics_update events (max events per second = 1/interval)
CCBT_EVENT_BUS_THROTTLE_MONITORING_HEARTBEAT=1.0  # Throttle interval for monitoring_heartbeat events (max events per second = 1/interval)
CCBT_LOG_FILE=  # Log file path (empty = stdout)
CCBT_LOG_LEVEL=INFO  # Log level (DEBUG/TRACE/INFO/WARNING/ERROR/CRITICAL)
CCBT_LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s  # Log format string
CCBT_LOG_CORRELATION_ID=true  # Include correlation IDs (true/false)
CCBT_METRICS_PORT=64125  # Metrics port (1024-65535)
CCBT_METRICS_INTERVAL=5.0  # Metrics collection interval in seconds (0.5-3600.0)
CCBT_STRUCTURED_LOGGING=true  # Use structured logging (true/false)
CCBT_TRACE_FILE=  # Path to write traces (empty = disabled)

# =============================================================================
# DAEMON CONFIGURATION
# =============================================================================

CCBT_DAEMON_IPC_HOST=  # IPC server host (127.0.0.1 for local-only access, 0.0.0.0 for all interfaces)
CCBT_DAEMON_IPC_PORT=  # IPC server port (1-65535)

# =============================================================================
# NAT CONFIGURATION
# =============================================================================

CCBT_NAT_AUTO_MAP_PORTS=true  # Automatically map ports on startup
CCBT_NAT_DISCOVERY_INTERVAL=300.0  # NAT device discovery interval in seconds
CCBT_NAT_ENABLE_NAT_PMP=true  # Enable NAT-PMP protocol
CCBT_NAT_ENABLE_UPNP=true  # Enable UPnP IGD protocol
CCBT_NAT_MAP_DHT_PORT=true  # Map DHT UDP port
CCBT_NAT_MAP_TCP_PORT=true  # Map TCP listen port
CCBT_NAT_MAP_UDP_PORT=true  # Map UDP listen port
CCBT_NAT_MAP_XET_MULTICAST_PORT=false  # Map XET multicast UDP port (usually not needed for multicast)
CCBT_NAT_MAP_XET_PORT=true  # Map XET protocol UDP port
CCBT_NAT_PORT_MAPPING_LEASE_TIME=3600  # Port mapping lease time in seconds

# =============================================================================
# WEBTORRENT CONFIGURATION
# =============================================================================

CCBT_WEBTORRENT_PORT=64126  # WebSocket signaling server port (1024-65535)

# =============================================================================
# DASHBOARD CONFIGURATION
# =============================================================================

CCBT_DASHBOARD_DEFAULT_VIEW=overview  # Default dashboard view (overview|performance|network|security|alerts)
CCBT_DASHBOARD_ENABLE=true  # Enable built-in dashboard/web UI (true/false)
CCBT_DASHBOARD_ENABLE_GRAFANA_EXPORT=false  # Enable Grafana dashboard JSON export endpoints (true/false)
CCBT_DASHBOARD_HOST=127.0.0.1  # Dashboard bind host
CCBT_DASHBOARD_PORT=9090  # Dashboard HTTP port (1024-65535)
CCBT_DASHBOARD_REFRESH_INTERVAL=1.0  # UI refresh interval in seconds (0.1-10.0)
CCBT_DASHBOARD_TERMINAL_CONNECTION_CHECK_INTERVAL=0.5  # Interval in seconds for checking daemon connection status (0.1-5.0)
CCBT_DASHBOARD_TERMINAL_CONNECTION_TIMEOUT=10.0  # Timeout in seconds for connecting to daemon after verification (1.0-60.0)
CCBT_DASHBOARD_TERMINAL_DAEMON_CHECK_INTERVAL=1.0  # Interval in seconds for checking daemon readiness during startup (0.1-10.0)
CCBT_DASHBOARD_TERMINAL_DAEMON_INITIAL_WAIT=5.0  # Initial wait time in seconds for IPC server to be ready (1.0-30.0)
CCBT_DASHBOARD_TERMINAL_DAEMON_RETRY_DELAY=0.5  # Delay in seconds between daemon readiness retry attempts (0.1-5.0)
CCBT_DASHBOARD_TERMINAL_DAEMON_STARTUP_TIMEOUT=90.0  # Timeout in seconds for daemon startup checks (10.0-300.0) - includes NAT discovery, DHT bootstrap, IPC server startup
CCBT_DASHBOARD_TERMINAL_REFRESH_INTERVAL=1.0  # Terminal dashboard UI refresh interval in seconds (0.5-10.0) - longer than web since WebSocket provides real-time updates

# =============================================================================
# QUEUE CONFIGURATION
# =============================================================================

CCBT_AUTO_MANAGE_QUEUE=true  # Automatically start/stop torrents based on queue limits (true/false)
CCBT_BANDWIDTH_ALLOCATION_MODE=proportional  # Bandwidth allocation strategy (proportional|equal|fixed|manual)
CCBT_DEFAULT_PRIORITY=normal  # Default priority for new torrents (paused|low|normal|high|maximum)
CCBT_MAX_ACTIVE_DOWNLOADING=3  # Maximum active downloading torrents (0 = unlimited)
CCBT_MAX_ACTIVE_SEEDING=2  # Maximum active seeding torrents (0 = unlimited)
CCBT_MAX_ACTIVE_TORRENTS=5  # Maximum number of active torrents (1-1000)

# =============================================================================
# PROXY CONFIGURATION
# =============================================================================

CCBT_PROXY_BYPASS_LIST=  # Comma-separated list of hosts/IPs to bypass proxy
CCBT_PROXY_ENABLE_PROXY=false  # Enable proxy support (true/false)
CCBT_PROXY_FOR_PEERS=false  # Use proxy for peer connections (true/false)
CCBT_PROXY_FOR_TRACKERS=true  # Use proxy for tracker requests (true/false)
CCBT_PROXY_FOR_WEBSEEDS=true  # Use proxy for WebSeed requests (true/false)
CCBT_PROXY_HOST=  # Proxy server hostname or IP
CCBT_PROXY_PASSWORD=  # Proxy password (encrypted in storage)
CCBT_PROXY_PORT=  # Proxy server port (1-65535)
CCBT_PROXY_TYPE=http  # Proxy type: http/socks4/socks5
CCBT_PROXY_USERNAME=  # Proxy username for authentication

# =============================================================================
# UI CONFIGURATION
# =============================================================================

CCBT_LOCALE=en  # Language/locale code (e.g., 'en', 'es', 'fr')
CCBT_UI_LOCALE=en  # Language/locale code (e.g., 'en', 'es', 'fr')

# =============================================================================
# XET_SYNC CONFIGURATION
# =============================================================================

CCBT_XET_SYNC_ALLOWLIST_ENCRYPTION_KEY=  # Path to allowlist encryption key file
CCBT_XET_SYNC_ALLOWLIST_PATH=  # Default allowlist path for workspace authorization
CCBT_XET_SYNC_AUTH_SCOPE=strict_workspace_auth  # Workspace auth scope (strict_workspace_auth/content_addressable_open)
CCBT_XET_SYNC_AUTO_ELECT_SOURCE=false  # Automatically elect source peer
CCBT_XET_SYNC_BYZANTINE_FAULT_THRESHOLD=0.33  # Byzantine fault threshold (max fraction of faulty nodes)
CCBT_XET_SYNC_CHECK_INTERVAL=5.0  # Interval between folder checks in seconds
CCBT_XET_SYNC_CONFLICT_RESOLUTION_STRATEGY=last_write_wins  # Conflict resolution strategy (last_write_wins/version_vector/three_way_merge/timestamp)
CCBT_XET_SYNC_CONSENSUS_ALGORITHM=simple  # Consensus algorithm (simple/raft)
CCBT_XET_SYNC_CONSENSUS_THRESHOLD=0.5  # Majority threshold for consensus mode (0.0 to 1.0)
CCBT_XET_SYNC_DEFAULT_SYNC_MODE=best_effort  # Default synchronization mode (designated/best_effort/broadcast/consensus)
CCBT_XET_SYNC_ENABLE_BYZANTINE_FAULT_TOLERANCE=false  # Enable Byzantine fault tolerance
CCBT_XET_SYNC_ENABLE_GIT_VERSIONING=true  # Enable git integration for version tracking
CCBT_XET_SYNC_ENABLE_GOSSIP=true  # Enable gossip protocol for update propagation
CCBT_XET_SYNC_ENABLE_LPD=true  # Enable Local Peer Discovery (BEP 14)
CCBT_XET_SYNC_ENABLE_XET=false  # Enable XET folder synchronization globally
CCBT_XET_SYNC_FLOODING_PRIORITY_THRESHOLD=100  # Priority threshold for using flooding (0-1000)
CCBT_XET_SYNC_FLOODING_TTL=10  # Controlled flooding TTL (max hops)
CCBT_XET_SYNC_GIT_AUTO_COMMIT=false  # Automatically commit changes on folder updates
CCBT_XET_SYNC_GOSSIP_FANOUT=3  # Gossip fanout (number of peers to gossip to)
CCBT_XET_SYNC_GOSSIP_INTERVAL=5.0  # Gossip interval in seconds
CCBT_XET_SYNC_HASH_ALGORITHM_POLICY=negotiate  # Hash identity policy (negotiate/require_configured)
CCBT_XET_SYNC_MAX_UPDATE_QUEUE_SIZE=100  # Maximum number of queued updates
CCBT_XET_SYNC_RAFT_ELECTION_TIMEOUT=1.0  # Raft election timeout in seconds
CCBT_XET_SYNC_RAFT_HEARTBEAT_INTERVAL=0.1  # Raft heartbeat interval in seconds
CCBT_XET_SYNC_REQUIRE_SIGNED_METADATA=true  # Require signed XET metadata and handshake identity when auth is enabled
CCBT_XET_SYNC_SOURCE_ELECTION_INTERVAL=300.0  # Source peer election interval in seconds
CCBT_XET_SYNC_WEIGHTED_VOTING=false  # Use weighted voting for consensus

# =============================================================================
# OPTIMIZATION CONFIGURATION
# =============================================================================

CCBT_OPTIMIZATION_EFFICIENCY_CONNECTION_LIMIT_MULTIPLIER=0.8  # Connection limit multiplier for efficiency profile (reduces connections for efficiency)
CCBT_OPTIMIZATION_ENABLE_ADAPTIVE_INTERVALS=true  # Enable adaptive discovery intervals based on swarm health
CCBT_OPTIMIZATION_ENABLE_BANDWIDTH_AWARE_SCHEDULING=true  # Enable bandwidth-aware piece request scheduling
CCBT_OPTIMIZATION_ENABLE_PERFORMANCE_BASED_RECYCLING=true  # Enable performance-based peer connection recycling
CCBT_OPTIMIZATION_LOW_RESOURCE_MAX_CONNECTIONS=20  # Maximum connections for low_resource profile
CCBT_OPTIMIZATION_PROFILE=balanced  # Optimization profile to use
CCBT_OPTIMIZATION_SPEED_AGGRESSIVE_PEER_RECYCLING=true  # Aggressively recycle low-performing peers in speed profile

# =============================================================================
# LEGACY / UNMAPPED ENVIRONMENT VARIABLES
# =============================================================================
# These keys are retained for backward compatibility and are not mapped
# in ConfigManager._get_env_config.

CCBT_ALERTS_RULES_PATH=.ccbt/alerts.json  # Path to alert rules JSON file | legacy/unmapped compatibility variable
CCBT_AUTO_DELETE_CHECKPOINT_ON_COMPLETE=true  # Auto-delete checkpoint when download completes (true/false) | legacy/unmapped compatibility variable
CCBT_CACHE_SIZE_MB=256  # Cache size in MB (16-4096) | legacy/unmapped compatibility variable
CCBT_CHECKPOINT_FORMAT=both  # Checkpoint file format (json/binary/both) | legacy/unmapped compatibility variable
CCBT_CHECKPOINT_INTERVAL=30.0  # Checkpoint save interval in seconds (1.0-3600.0) | legacy/unmapped compatibility variable
CCBT_CHECKPOINT_ON_PIECE=true  # Save checkpoint after each verified piece (true/false) | legacy/unmapped compatibility variable
CCBT_CHECKPOINT_RETENTION_DAYS=30  # Days to retain checkpoints before cleanup (1-365) | legacy/unmapped compatibility variable
CCBT_DAEMON_AUTO_SAVE_INTERVAL=60.0  # Auto-save state interval in seconds (1.0-3600.0) | legacy/unmapped compatibility variable
CCBT_DAEMON_TLS_ENABLED=false  # Enable TLS/HTTPS for IPC server (true/false) | legacy/unmapped compatibility variable
CCBT_DAEMON_WEBSOCKET_ENABLED=true  # Enable WebSocket support (true/false) | legacy/unmapped compatibility variable
CCBT_DAEMON_WEBSOCKET_HEARTBEAT_INTERVAL=30.0  # WebSocket heartbeat interval in seconds (1.0-300.0) | legacy/unmapped compatibility variable
CCBT_DEBUG=false  # Enable debug mode (true/false) | legacy/unmapped compatibility variable
CCBT_DHT_TIMEOUT=2.0  # DHT request timeout (1.0-60.0) | legacy/unmapped compatibility variable
CCBT_DISK_QUEUE_SIZE=200  # Disk I/O queue size (10-1000) | legacy/unmapped compatibility variable
CCBT_DOWNLOAD_PATH=  # Default download path | legacy/unmapped compatibility variable
CCBT_ENABLE_IO_URING=false  # Enable io_uring on Linux if available (true/false) | legacy/unmapped compatibility variable
CCBT_ENABLE_IPV6=true  # Enable IPv6 support (true/false) | legacy/unmapped compatibility variable
CCBT_ENABLE_PROFILING=false  # Enable performance profiling (true/false) | legacy/unmapped compatibility variable
CCBT_ENABLE_TCP=true  # Enable TCP transport (true/false) | legacy/unmapped compatibility variable
CCBT_ENABLE_UTP=false  # Enable uTP transport (true/false) | legacy/unmapped compatibility variable
CCBT_ENABLE_WEBTORRENT=false  # Enable WebTorrent protocol support (true/false) | legacy/unmapped compatibility variable
CCBT_FIRST_PIECE_PRIORITY=true  # Prioritize first piece (true/false) | legacy/unmapped compatibility variable
CCBT_HASH_BATCH_SIZE=4  # Number of pieces to verify in parallel batches (1-64) | legacy/unmapped compatibility variable
CCBT_HASH_CHUNK_SIZE=65536  # Chunk size for hash verification (1024-1048576) | legacy/unmapped compatibility variable
CCBT_HASH_QUEUE_SIZE=100  # Hash verification queue size (10-500) | legacy/unmapped compatibility variable
CCBT_LAST_PIECE_PRIORITY=false  # Prioritize last piece (true/false) | legacy/unmapped compatibility variable
CCBT_LIMITS_GLOBAL_DOWN_KIB=0  # Global download limit (0+) | legacy/unmapped compatibility variable
CCBT_LIMITS_GLOBAL_UP_KIB=0  # Global upload limit (0+) | legacy/unmapped compatibility variable
CCBT_LIMITS_PER_PEER_UP_KIB=0  # Per-peer upload limit (0+) | legacy/unmapped compatibility variable
CCBT_LIMITS_PER_TORRENT_DOWN_KIB=0  # Per-torrent download limit (0+) | legacy/unmapped compatibility variable
CCBT_LIMITS_PER_TORRENT_UP_KIB=0  # Per-torrent upload limit (0+) | legacy/unmapped compatibility variable
CCBT_LISTEN_INTERFACE=0.0.0.0  # Listen interface | legacy/unmapped compatibility variable
CCBT_MAX_BLOCK_SIZE_KIB=64  # Maximum block size in KiB (1-1024) | legacy/unmapped compatibility variable
CCBT_MIN_BLOCK_SIZE_KIB=4  # Minimum block size in KiB (1-64) | legacy/unmapped compatibility variable
CCBT_ML_PEER_SELECTION_ENABLED=false  # Enable ML-based peer selection (true/false) | legacy/unmapped compatibility variable
CCBT_ML_PIECE_PREDICTION_ENABLED=false  # Enable ML piece prediction (true/false) | legacy/unmapped compatibility variable
CCBT_MMAP_CACHE_CLEANUP_INTERVAL=30.0  # MMap cache cleanup interval (1.0-300.0) | legacy/unmapped compatibility variable
CCBT_MOCK_NETWORK=false  # Use mock network for testing (true/false) | legacy/unmapped compatibility variable
CCBT_OPTIMISTIC_UNCHOKE_INTERVAL=30.0  # Optimistic unchoke interval (1.0-600.0) | legacy/unmapped compatibility variable
CCBT_PEER_TIMEOUT=60.0  # Peer inactivity timeout (5.0-600.0) | legacy/unmapped compatibility variable
CCBT_PIPELINE_CAPACITY=4  # Request pipeline capacity (1-32) | legacy/unmapped compatibility variable
CCBT_PROFILE_OUTPUT=  # Profile output file path | legacy/unmapped compatibility variable
CCBT_QUEUE_STATE_SAVE_INTERVAL=30.0  # Interval to save queue state in seconds (5.0-3600.0) | legacy/unmapped compatibility variable
CCBT_RAREST_FIRST_THRESHOLD=0.1  # Rarest first threshold (0.0-1.0) | legacy/unmapped compatibility variable
CCBT_SAVE_QUEUE_STATE=true  # Save queue state to checkpoint (true/false) | legacy/unmapped compatibility variable
CCBT_SCHEDULER_SLICE_MS=100  # Scheduler time slice in ms (1-1000) | legacy/unmapped compatibility variable
CCBT_SEQUENTIAL_FALLBACK_THRESHOLD=0.1  # Fallback to rarest-first if availability < threshold (0.0-1.0) | legacy/unmapped compatibility variable
CCBT_SEQUENTIAL_PRIORITY_FILES=  # File paths to prioritize in sequential mode (comma-separated, optional) | legacy/unmapped compatibility variable
CCBT_SEQUENTIAL_WINDOW=10  # Sequential window size (1-100) | legacy/unmapped compatibility variable
CCBT_SOCKET_RCVBUF_KIB=256  # Socket receive buffer size in KiB (1-65536) | legacy/unmapped compatibility variable
CCBT_SOCKET_SNDBUF_KIB=256  # Socket send buffer size in KiB (1-65536) | legacy/unmapped compatibility variable
CCBT_SPARSE_FILES=false  # Use sparse files if supported (true/false) | legacy/unmapped compatibility variable
CCBT_TCP_NODELAY=true  # Enable TCP_NODELAY (true/false) | legacy/unmapped compatibility variable
CCBT_TEST_MODE=false  # Enable test mode (true/false) | legacy/unmapped compatibility variable
CCBT_TRACKER_CONNECTIONS_PER_HOST=10  # Max connections per tracker host (1-50) | legacy/unmapped compatibility variable
CCBT_TRACKER_CONNECTION_LIMIT=50  # Maximum tracker connections (1-200) | legacy/unmapped compatibility variable
CCBT_TRACKER_CONNECT_TIMEOUT=10.0  # Tracker connection timeout (1.0-60.0) | legacy/unmapped compatibility variable
CCBT_UNCHOKE_INTERVAL=10.0  # Unchoke interval (1.0-600.0) | legacy/unmapped compatibility variable
CCBT_VERBOSE=false  # Enable verbose output (true/false) | legacy/unmapped compatibility variable
CCBT_WEBTORRENT_CONNECTION_TIMEOUT=30.0  # WebRTC connection timeout in seconds (5.0-120.0) | legacy/unmapped compatibility variable
CCBT_WEBTORRENT_HOST=localhost  # WebSocket signaling server host | legacy/unmapped compatibility variable
CCBT_WEBTORRENT_MAX_CONNECTIONS=100  # Maximum WebRTC connections (1-1000) | legacy/unmapped compatibility variable
CCBT_WRITE_BUFFER_KIB=1024  # Write buffer size in KiB (0-65536) | legacy/unmapped compatibility variable

# Manual verification (MSE): run ``btbt config security-posture``; expect security.enable_encryption true when
# configured. In session logs, expect ``mse_enabled=True`` on the startup security posture line and
# ``mse_plain_fallback=`` matching encryption_allow_plain_fallback. With MSE enabled, DEBUG lines
# ``Outbound plaintext preferred ... skipping MSE`` should be rare (only when policy resolves to DISABLED).
